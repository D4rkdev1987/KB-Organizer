<%= form_with(model: part_time, local: true) do |form| %>
  <% if part_time.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(part_time.errors.count, "error") %> prohibited this part_time from being saved:</h2>

      <ul>
      <% part_time.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :part_time_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
